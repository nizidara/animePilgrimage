import {memo, FC} from "react";
import { Container } from "react-bootstrap";

export const PlaceList: FC = memo(() =>{
    return (
        <Container>
            <h2>聖地一覧ページです．</h2>
        </Container>
    )
});