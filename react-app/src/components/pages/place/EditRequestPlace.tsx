import {memo, FC} from "react";
import { Container } from "react-bootstrap";

export const EditRequestPlace: FC = memo(() =>{
    return (
        <Container>
            <h2>聖地修正ページです．</h2>
        </Container>
    )
});