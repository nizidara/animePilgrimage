import {memo, FC} from "react";
import { Container } from "react-bootstrap";

export const Login: FC = memo(() =>{
    return (
        <Container>
            <h2>ログインページです．</h2>
        </Container>
    )
});