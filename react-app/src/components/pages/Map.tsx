import {memo, FC} from "react";
import { Container } from "react-bootstrap";

export const Map: FC = memo(() =>{
    return (
        <Container>
            <h2>Map表示ページです．</h2>
        </Container>
    )
});