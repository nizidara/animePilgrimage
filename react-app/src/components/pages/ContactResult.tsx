import {memo, FC} from "react";
import { Container } from "react-bootstrap";

export const ContactResult: FC = memo(() =>{
    return (
        <Container>
            <h2>お問い合わせ完了ページです．</h2>
        </Container>
    )
});