export type deletePlace ={
    contents: string;
}

export type registerPlace ={
    name: string;
    animeId: number;
    regionId: number;
    comment: string;
}

export type photoData = {
    title: string;
    name: string;
}